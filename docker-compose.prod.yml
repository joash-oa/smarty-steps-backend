services:
  api:
    image: ghcr.io/joash-oa/smarty-steps-backend:${IMAGE_TAG:-latest}
    ports:
      - "8000:8000"
    env_file: .env
    restart: unless-stopped
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 2
